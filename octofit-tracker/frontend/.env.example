# Frontend Configuration
# Codespaces: set this to the current workspace name in .env.local.
VITE_CODESPACE_NAME=your-codespace-name

# Local fallback when VITE_CODESPACE_NAME is unset.
VITE_API_URL=http://localhost:8000
